import {Box} from "@material-ui/core";
import UserForm from "../../../components/Admin/User/UserForm";

const Create = () => {
    return (
        <Box my={2}>
            <UserForm title='ایجاد کاربر جدید'/>
        </Box>

    );
};

export default Create;